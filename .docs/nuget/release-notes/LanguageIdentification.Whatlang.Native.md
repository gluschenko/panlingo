0.8.0
- Harden native Whatlang bindings with explicit input lengths and argument validation
- Use stable byte-sized language and script values across the FFI boundary
- Add native status handling for invalid enum values
- Make native string-copy helpers respect destination buffer sizes
- Add NuGet package icon and expanded package tags


