0.8.0
- Harden native Lingua bindings with explicit input lengths and argument validation
- Use stable byte-sized language values across the FFI boundary
- Add native status codes for invalid detector pointers and enum values
- Improve prediction result memory cleanup
- Add NuGet package icon and expanded package tags


